import { StorePipePipe } from './store-pipe.pipe';

describe('StorePipePipe', () => {
  it('create an instance', () => {
    const pipe = new StorePipePipe();
    expect(pipe).toBeTruthy();
  });
});
